export const UNITS = {
    celsius:'Celsius',
    fahrenheit:'Fahrenheit'
}

const initModel = {
  leftTempValue:0,
  leftTempUnit:UNITS.celsius,
  rightTempValue:32,
  rightTempUnit:UNITS.fahrenheit,
  sourceLeft:true
};

export default initModel;
